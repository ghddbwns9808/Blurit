<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Blurit" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="S">@android:color/transparent</item>
    </style>

    <style name="Theme.Blurit" parent="Base.Theme.Blurit" />

    <style name="Blurit.Text" parent="Theme.Blurit">
        <item name="android:textColor">@color/blurit_pink</item>
        <item name="fontFamily">@font/pretendard_medium</item>
    </style>

</resources>